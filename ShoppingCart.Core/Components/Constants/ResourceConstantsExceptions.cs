namespace ShoppingCart.Core.Components.Constants
{
    public static class ResourceConstantsExceptions
    {
        public static string NotFound(string key) => $"{key} not found";
        public static string NotValid(string key) => $"{key} not valid";
    }
}